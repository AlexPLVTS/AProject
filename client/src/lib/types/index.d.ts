type Activity = {
    id: string
    title: string
    date: date
    description: string
    category: string
    city: string
    venue: string
}

type User = {
    id: string
    email: string
    displayName: string
    imageUrl?: string
}